# Path to your Obsidian vault root (the folder that contains .obsidian/).
# Prefer an absolute path. ~ is OK — the Makefile expands it.
OBSIDIAN_VAULT=/path/to/YourVault

# Demo vault for screenshots (repo-relative): make sync-demo
# OBSIDIAN_VAULT=/path/to/obsidian-timeflow/demo-vault
